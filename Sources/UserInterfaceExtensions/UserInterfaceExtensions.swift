
/// UserInterfaceExtensions contains commonly used ViewModifiers and additional SwiftUI convenience functions.
///
/// - Animations:
///     * All animation modifiers.
/// - Basics:
///     * Basic modifications like convenience functions to set .frame(maxWidth: .infinity), etc.
///     * Platform modifiers to segment code for iOS / Mac
///     * GeometryReader convenience modifiers.
/// - Glass:
///     * Supporting glass for '26+ devices, with fallbacks for pre-26 devices.
/// - GestureSupport:
///     * ShakeGesture support in SwiftUI.
/// - Email:
///     * ViewModifier to support UIKit's `MFMailComposeViewController` as a SwiftUI modifier.

